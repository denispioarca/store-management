/**
 * Domain services containing core business logic that cannot naturally
 * live inside a single domain model.
 *
 * These services operate on domain models and enforce business invariants.
 */
package com.example.store_management.domain.service;